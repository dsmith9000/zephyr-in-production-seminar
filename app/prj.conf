CONFIG_GPIO=y
CONFIG_LOG=y

CONFIG_SHELL=y
CONFIG_ASSERT=y

# Enable support for MCUboot (or enable in and use sysbuild.conf with 'west --sysbuild' flag)
CONFIG_BOOTLOADER_MCUBOOT=y
# Specify key file to use for image signing
CONFIG_MCUBOOT_SIGNATURE_KEY_FILE="${APP_DIR}/../keys/my-key.pem"
